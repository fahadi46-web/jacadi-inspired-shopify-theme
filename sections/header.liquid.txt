<header class="site-header">
  <div class="header-inner">
    <button class="mobile-menu-toggle">☰</button>

    <a href="/">
      <img src="{{ 'logo.svg' | asset_url }}" height="38" alt="{{ shop.name }}">
    </a>

    <nav class="main-nav">
      {% for link in linklists.main-menu.links %}
        <div class="nav-item">
          <a href="{{ link.url }}">{{ link.title }}</a>
          {% if link.links != blank %}
          <div class="mega-menu">
            <div class="mega-grid">
              {% for child in link.links %}
                <div>
                  <strong>{{ child.title }}</strong>
                  <ul>
                    {% for grand in child.links %}
                      <li><a href="{{ grand.url }}">{{ grand.title }}</a></li>
                    {% endfor %}
                  </ul>
                </div>
              {% endfor %}
            </div>
          </div>
          {% endif %}
        </div>
      {% endfor %}
    </nav>

    <button class="cart-toggle">👜</button>
  </div>
</header>

{% render 'cart-drawer' %}
{% render 'mobile-menu' %}
